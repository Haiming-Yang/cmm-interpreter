/**
 * 测试 变量声明
 * 2010-11-13
 * 邱忠磊
 * 
 * 测试变量声明是否合法 主要是：
 * 1.在相同作用域内不允许出现同名变量
 * 2.不同作用域内隐藏上级同名变量
 */

//测试标识符是否合法
int a_2;
real r_2_r;
real i,j=23.0,k;


//测试不同作用域内同名变量隐藏
int c = 23;
int b[2];
b[0] = 23;
b[1] = 0;
if(b[1] == 0)
{
	int c = 24;
	if(c == 24)
		write(1);
	else
		write(0);

	int b[1];
	b[0] = 5;
	if(b[0] == 5)
		write(1);
	else
		write(0);
}
/*output:
1
1
*///:~